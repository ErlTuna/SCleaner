public static class PickupFactory
{
    /*
    public PickupRuntimeData CreatePickupFromItem(IItemRuntimeData itemData)
    {
        // Create runtime data for the pickup, tracking the source
        var pickupData = new PickupRuntimeData(itemData.Config, itemData);

        // Copy exhaustion state from the original item if available
        if (itemData is PickupRuntimeData runtimeSource)
        {
            for (int i = 0; i < pickupData.Behaviors.Length; i++)
            {
                pickupData.Behaviors[i].IsExhausted = runtimeSource.Behaviors[i].IsExhausted;
            }
        }

        return pickupData;
    }
    */
}
