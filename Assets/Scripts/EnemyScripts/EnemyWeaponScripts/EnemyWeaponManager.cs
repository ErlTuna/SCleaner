using UnityEngine;

public class EnemyWeaponManager : MonoBehaviour
{
    /*
    BaseWeapon _weapon;

    public void FireOnce()
    {
        _weapon.TryFireDirectly();
    }

    public void FirePattern(AttackPatternSO pattern, MonoBehaviour runner)
    {
        runner.StartCoroutine(pattern.Execute(_weapon));
    }

    public void ReloadIfNeeded()
    {
        // Optional if enemies actually reload
        if (!_weapon.AmmoManager.HasAmmo())
            _weapon.HandleReloadInput();
    }

    */

}
