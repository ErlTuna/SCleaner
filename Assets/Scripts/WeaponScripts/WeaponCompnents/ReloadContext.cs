using System;

// Small DTO to be used in ReloadStrategies.

/*
public class ReloadContext : IReloadContext
{
    public WeaponRuntimeAmmoData AmmoRuntime {get;}
    public IAmmoConfig AmmoConfig {get;}
    //public WeaponState State {get; set;}

    // Callbacks for weapon-specific events, optional.
    //public Action OnReloadAnimEnd { get; }
    //public Action OnAttackAnimEnd { get; }

    public ReloadContext
    (
        WeaponRuntimeAmmoData ammoRuntime,
        WeaponAmmoConfigSO ammoConfig,
        IWeaponAnimator animator,
        WeaponState state
        //Action onReloadEnd = null,
        //Action onAttackAnimEnd = null
    )
    {
        AmmoRuntime = ammoRuntime;
        AmmoConfig = ammoConfig;
    }
}

*/




